services:
  - type: web
    name: spend-log-frontend
    env: static
    plan: free
    buildCommand: npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_BASE_URL
        value: https://spend-log-backend.onrender.com/api